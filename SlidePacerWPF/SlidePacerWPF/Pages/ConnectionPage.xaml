<UserControl x:Class="SlidePacerWPF.Pages.ConnectionPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Loaded="UserControl_Loaded">
    
    <Grid Style="{StaticResource ContentRoot}" Margin="0">
        <ScrollViewer VerticalAlignment="Top">
            <GroupBox Header="Connect to Interpreter's App" HorizontalAlignment="Left" Margin="0,10,0,0" VerticalAlignment="Top" Padding="10,15,10,10" Grid.IsSharedSizeScope="True" FontWeight="Bold" Width="297" FontSize="16">
                <StackPanel Orientation="Horizontal" MinWidth="200">
                    <TextBox x:Name="tbAddress" Height="25" TextWrapping="NoWrap" Text="10:3B:59:E0:09:B7" Width="122" FontWeight="Normal" FontSize="12"/>
                    <Button x:Name="btnConnect" Height="25" Margin="5,0,0,0" BorderThickness="0" Content="Connect" Click="btnConnect_Click" FontWeight="Normal" FontSize="12"/>
                </StackPanel>
            </GroupBox>
        </ScrollViewer>

    </Grid>
</UserControl>
